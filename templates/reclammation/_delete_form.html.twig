<form method="post" action="{{ path('reclammation_delete', {'id': reclammation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclammation.id) }}">
    <button class="btn">Delete</button>
</form>
