<form method="post" action="{{ path('blogueur_delete', {'id': blogueur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blogueur.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
